services:
  mongo:
    image: "mongo"
    ports:
      - "127.0.0.1:27017:27017"
    restart: "always"

  visualizer:
    build:
      context: ./
      dockerfile: docker/Dockerfile
    ports:
      - "127.0.0.1:5000:5000"

